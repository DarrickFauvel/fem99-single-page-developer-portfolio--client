---
---

<section id="contact">
  <div class="container">
    <h2>Contact</h2>
    <p>
      I would love to hear about your project and how I could help. Please fill
      in the form, and I’ll get back to you as soon as possible.
    </p>

    <form action="">
      <input type="text" name="name" placeholder="Name" autocomplete="none" />
      <input type="text" name="email" placeholder="Email" autocomplete="none" />
      <textarea
        name="message"
        id="message"
        cols="30"
        rows="10"
        placeholder="Message"></textarea>

      <button class="btn highlight">Send message</button>
    </form>
  </div>
</section>

<style>
  section {
    background-color: var(--color-eerie-black);
    padding-block: calc((60 / 40) * 1em);
  }

  h2,
  p {
    text-align: center;
  }

  h2 {
    font-size: calc((40 / 16) * 1rem);
    line-height: calc(40 / 40);
    letter-spacing: -1.14px;
  }

  p {
    margin-top: calc((20 / 16) * 1em);
  }

  form {
    display: grid;
    gap: calc((32 / 16) * 1em);
    margin-top: calc((50 / 16) * 1em);
  }

  input,
  textarea {
    background-color: transparent;
    color: var(--color-white);
    border: none;
    border-bottom: 1px solid var(--color-white);
    text-transform: uppercase;
    font-family: inherit;
    padding-inline: calc((24 / 16) * 1em);
    padding-bottom: calc((17 / 16) * 1em);
  }
  input::placeholder,
  textarea::placeholder {
    color: var(--color-platinum);
  }

  input:focus {
    border-bottom: 1px solid var(--color-aquamarine);
    outline: none;
  }

  input {
    height: calc((43 / 16) * 1em);
  }

  textarea {
    height: calc((107 / 16) * 1em);
  }

  button {
    background-color: transparent;
    border: none;
    padding-bottom: 8px;
    border-bottom: 2px solid var(--color-aquamarine);
    text-transform: uppercase;
    font-family: inherit;
    color: var(--color-white);
    display: flex;
    width: max-content;
    justify-self: self-end;
    letter-spacing: 2.29px;
    line-height: 26px;
  }
</style>
